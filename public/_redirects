# Cloudflare Pages Redirects & SPA Routing Configuration
# Note: Cloudflare Pages handles Single Page Application (SPA) fallback via dist/404.html.
# Do NOT include recursive rules like "/* /index.html 200" as Cloudflare flags them as infinite loops.
